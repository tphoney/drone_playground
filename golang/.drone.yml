kind: pipeline
type: docker
name: default

pipeline:
  golang_test:
    image: golang:latest
    commands:
      - pwd
      - ls -al
      - cd golang
      - ls -al
      - go vet
      - go test -v -cover
  golang_build:
    image: golang:latest
    commands:
      - cd golang
      - go build